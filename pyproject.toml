[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fieldcore"
version = "0.1.0"
description = "Core library for field monitoring devices"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
